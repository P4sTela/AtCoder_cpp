#include <bits/stdc++.h>

using namespace std;

int main(){
  int n;
  cin >> n;
  vector<long long> t(n), x(n), a(n);
  for (int i = 0; i < n; ++i) {
    cin >> t.at(i) >> x.at(i) >> a.at(i);
  }

  int time=0;
  int np=0;

}
